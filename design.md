# 连点器：移动端界面设计

## 设计目标

本应用是一款 Android 优先的多点连点器。用户可以在应用内创建多个带编号的圆形点击点，调整每个点的点击参数，保存为方案并生成可复制的分享代码；获得系统授权后，连点器通过悬浮窗控制球和前台服务在离开应用后继续运行。界面按竖屏 9:16 和单手操作设计，优先保证底部控制区与右侧点击点工具条的可达性。

## Screen List

| 页面或层级 | 主要内容与功能 |
|---|---|
| 首次欢迎说明 | 展示创作者致意与免责声明，用户点击“开始使用”后进入编辑器；使用本地标记避免重复弹出。 |
| 连点器编辑器 | 顶部方案名称和运行状态，中部模拟画布，侧边纵向工具条，底部参数面板、保存和分享入口。 |
| 点击点参数面板 | 编辑当前选中点的编号、点击间隔、点击次数、长按时长等参数，并提供删除按钮。 |
| 保存方案面板 | 输入方案名称，查看已保存方案，覆盖保存或新建保存。 |
| 分享代码面板 | 生成随机数字与英文字符混合的短代码，复制或系统分享；输入他人代码后恢复公开分享的方案数据。 |
| Android 授权流程 | 从编辑器打开悬浮窗权限和无障碍设置；在服务启用后提示可返回启动前台服务。 |

## 编辑器布局

顶部使用深蓝色状态栏卡片显示“未运行 / 运行中 / 已暂停”和当前方案名。中部为浅灰蓝画布，点击点以 52pt 左右的圆形按钮呈现，中心显示创建顺序编号 `1`、`2`、`3`；选中点使用靛蓝描边和外圈光晕。右侧固定一列圆形工具按钮，从上到下为“添加点击点”“删除选中点”“参数设置”“保存方案”。底部使用半透明卡片承载全局间隔与目标循环次数，主操作按钮为“开始运行 / 暂停 / 停止”。

## 关键用户流程

| 用户动作 | 系统反馈 | 结果 |
|---|---|---|
| 点击“添加点击点” | 画布出现下一个编号的圆形点并自动选中 | 点位加入当前方案 |
| 拖动点击点 | 点位跟随手指并限制在画布边界内 | 更新该点坐标 |
| 点击工具栏“删除” | 选中点淡出，其后点位重新编号 | 当前点被移除，编号连续 |
| 点击“参数” | 打开底部编辑面板 | 修改间隔、次数和点位动作 |
| 点击“保存” | 出现名称输入与保存反馈 | 方案写入本地存储 |
| 点击“生成分享代码” | 显示随机英文/数字代码和复制按钮 | 用户可把代码粘贴到官方论坛 |
| 粘贴他人代码并导入 | 校验代码并预览方案 | 用户主动导入后可编辑、保存和使用 |
| 点击“开始运行” | 检查授权；已授权则启动悬浮球/前台服务，未授权则引导设置 | 退出应用后保留控制入口 |

## 性能提示

点击间隔低于 100ms、点击点数量较多或总点击频率较高时，在参数区域显示琥珀色风险提示：“当前频率较高，部分设备可能出现卡顿或耗电增加”。不阻断用户操作，但在开始运行前再次确认运行风险。服务应避免使用过密的事件监听，点位执行以用户保存的参数为准。

## 色彩与动效

| 元素 | 色值 | 用途 |
|---|---|---|
| 品牌靛蓝 | `#315EF4` | 主按钮、选中点击点、悬浮控制球 |
| 深夜蓝 | `#172554` | 状态卡片与主要文字 |
| 雾白 | `#F8FAFC` | 页面和画布背景 |
| 云灰 | `#E2E8F0` | 卡片边框、画布网格 |
| 青绿 | `#0F9E82` | 运行成功与服务已启用状态 |
| 琥珀 | `#D97706` | 性能风险和授权提醒 |

新增点位使用 180ms 的淡入与轻微缩放；选中点只使用 120ms 的描边和光晕变化；拖动过程不使用弹簧回弹，松手后以 180ms 缓动限制到画布边界。系统级悬浮球和通知由 Android 原生服务管理，不依赖 JavaScript 定时器在后台常驻。

## 可访问性与安全边界

工具条按钮均提供中文无障碍标签，触摸区域不小于 48pt。分享代码只包含用户主动生成的方案结构、点位和参数，不自动上传私有方案；论坛复用通过用户复制/粘贴代码完成。无障碍服务仅在用户明确开启后工作，并在前台通知中显示运行状态与停止入口。
